using UnityEngine;
using System.Collections;

public class dofController : MonoBehaviour {

	//DepthOfField dofScript;
	float speed;
	// Use this for initialization
	void Start () {
		//dofScript = gameObject.GetComponent<DepthOfField>();
	}

	// Update is called once per frame
	void Update () {
		//dofScript.maxBlurSize -= Time.deltaTime * speed;
	}
}
